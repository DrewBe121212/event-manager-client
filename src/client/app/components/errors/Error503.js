import React from 'react';
import { ErrorContainer } from './ErrorContainer';

export const Error503 = () => (
  <ErrorContainer title="Error 503">
    The server is not currently available.
  </ErrorContainer>
);